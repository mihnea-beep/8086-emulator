push 20; pop AX; mov CX, 3200; mov DX, 10; mov BP, 20; mov [CX], DX; mov AX, [CX]; inc AX; mov BX, AX; add bx, 200; sub bx, 50; sub dx, 10; add AX, 510; inc AX; mov AX, 60000; mov SI, 100; MOV AX, [BP + SI];
INC ecx; INC ecx; INC ecx; INC ecx; MOV eax, 600; ADD eax, ecx; ADD ecx, 100; DEC ecx;
DEC edx; DEC edx; MOV eax, 340; ADD ecx, eax; ADD eax, 2000;
